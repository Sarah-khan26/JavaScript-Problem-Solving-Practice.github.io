let point = 0;

function point(){
    point = point + 1;
    document.querySelector('p').innerHTML =point;
}

const passingMarks = 50;

function check(){
    let englishMarks = document.querySelector('english').value;
}